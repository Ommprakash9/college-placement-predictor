## Packages
framer-motion | Complex page transitions and entry animations
recharts | Data visualization for placement statistics and feature importance
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
lucide-react | Icons for the UI (already in base, but listing for completeness)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
